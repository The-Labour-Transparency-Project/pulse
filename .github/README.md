# GitHub configuration

Repository automation lives in `.github/workflows`. Workflows should validate
the documentation site, application and package checks as those projects
become executable, and infrastructure plans with appropriate review gates.
