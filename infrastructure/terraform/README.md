# Terraform

Terraform modules and environment composition for cloud resources. Keep state
remote and protected, review plans, and pass environment-specific values from
the environment directories rather than embedding secrets in code.
